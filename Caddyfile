{$APP_DOMAIN} {
    encode zstd gzip

    handle /health {
        reverse_proxy backend:4000
    }

    handle_path /api/* {
        reverse_proxy backend:4000
    }

    handle {
        reverse_proxy frontend:3000
    }
}